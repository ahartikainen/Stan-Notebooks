#!/bin/bash
set -ex

jupyter nbextension enable --py widgetsnbextension
jupyter labextension install @jupyter-widgets/jupyterlab-manager

conda activate stan
python -m pip install cmdstanpy[all]
python -m cmdstanpy.install_cmdstan
